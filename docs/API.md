# API 约定

## 当前状态

当前项目先用共享层里的 `fakeRequest` 模拟异步请求，目的是先稳定前端结构，再逐步替换成真实后端调用。

## 分层规则

- 领域服务层负责调用 API 并整理数据
- UI 组件不要直接发请求
- 共享层只提供通用请求工具，不承载业务规则
- 请求失败后的用户提示由 UI 层或共享消息中心负责触发

## 后续替换建议

当需要接入真实后端时：

- 在 `src/shared/lib` 中补充真实 HTTP 客户端
- 在 `src/domains/*/service` 中保留领域化数据整理
- 统一处理超时、错误消息和鉴权头
