# 状态管理

## 当前策略

这个项目暂时不引入额外状态管理库，而是按职责拆成两类状态：

- 本地页面状态：直接放在页面或组件内部
- 跨页面 UI 状态：放在共享层的轻量 store 中

## 当前已存在的共享状态

- 登录会话：`src/shared/lib/session.ts`
- 全局消息通知：`src/shared/lib/uiStore.ts`

## 使用原则

- 只有跨页面、跨组件复用的状态，才放进共享层
- 表单输入和页面加载状态优先放在领域 UI 内部
- 业务规则不要写进共享 store，应保留在领域 `service`
