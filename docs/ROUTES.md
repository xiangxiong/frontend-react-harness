# 路由说明

## 当前页面

- `/`：首页，说明 harness engineering 工作方式
- `/login`：登录页，演示最小认证流程
- `/console`：控制台页，受登录态保护

## 路由原则

- 路由编排放在 `src/app`
- 访问守卫放在 `src/app`
- 具体页面实现仍归各自领域模块负责

## 扩展建议

新增页面时，优先遵循：

- 先补产品说明和执行计划
- 再添加领域类型、服务和 UI
- 最后在 `src/app/router.tsx` 里登记路由
